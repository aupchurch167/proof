# B04 — Input hardening and dependency hygiene

**Includes:** A4-03, A4-04, A4-05, A4-06, A4-07, A4-08, A4-09, A4-10, A5-03

**Files:** `services/email.js`, `services/coiExtractor.js`, `utils/validation.js`, `routes/reports.js`, `routes/notifications.js`, `app.js`, `.gitignore`, `server/package.json`

## Work

1. HTML-escape email interpolations (A4-03).
2. Per-org daily extract cap + 4MB byte cap (A4-04).
3. zod on settings / vendor PUT / google / forgot / apply (A4-05).
4. `npm audit fix`; remove unused `nodemailer` (A4-06).
5. `%PDF-` magic bytes (A4-07).
6. `export-pdfs` max 50 (A4-08).
7. Railway CORS origin — **only if owner confirms unused** (A4-09).
8. `parseLimit` on notifications (A4-10).
9. gitignore `.env.*` (A5-03).

## Tests

Org name with `<img` escaped. 51 COI IDs → 400. `npm audit --omit=dev` high=0 or listed exceptions.
