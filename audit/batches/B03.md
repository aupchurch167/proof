# B03 — Destructive ops, cron durability, uniqueness

**Goal:** A MEMBER click or a missed cron tick cannot destroy history or double-email; DB uniqueness matches the app checks.

**Includes:** A3-02, A3-03 (start: Restrict on AuditLog only if full cascade is too big), A3-04, A3-07, A3-10, A2-12, A6-04

**Files:** `routes/vendors.js`, `routes/import.js`, `middleware/planLimits.js`, `services/cron.js`, `index.js`, `schema.prisma`

## Work

1. Soft-delete COIs (or skip `deleteFile` until retain window). Transaction around vendor delete (A3-02, A3-07).
2. Import calls `generateUploadToken` (A3-07).
3. Partial unique `(orgId, email)` where `deletedAt IS NULL` (A2-12).
4. Cron advisory lock / single-flight; `job.stop()` on SIGTERM (A3-04, A6-04).
5. `enforcePlanLimit` catch → 503 (A3-10).
6. Do **not** flip all Organization cascades in the same PR unless a migration plan exists (A3-03 → follow-up).

## Tests

Vendor+COI+NotificationLog delete 200 without 500. Parallel vendor create same email → one 409. Two overlapping sweeps, second no-ops.

## Owner pairing

Restore rehearsal (A3-01 / runbook) happens in parallel, not in this PR.
