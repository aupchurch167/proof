# B05 — Make the suite catch the next isolation bug

**Includes:** A6-01, A9-01, A9-02, A9-03, A9-05

**Files:** new `.github/workflows/test.yml`, `server/tests/*`, `tests/setup.js`, `app.js` / `main.jsx` for Sentry

## Work

1. GitHub Action: Node 22, `npm ci`, `cd server && npm test` against a disposable Postgres (A9-01).
2. Implement the Friday-deploy suite plus WH-1/V-01/C-02/O-03/L-02 from `/audit/tenant-isolation-tests.md` (A9-02).
3. Align `portal.test.js` with B02’s 401 (A9-03).
4. `cleanupTestData` requires `PROOF_TEST_DB=1` or a URL containing `_test` (A9-05).
5. Sentry Node + React (A6-01).

## Tests

CI red on a broken expect. A second org named `keep-me` survives cleanup if guard is on.
