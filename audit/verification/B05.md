# B05 verification (Prompt V)

**Verifier:** skeptical re-run against `fix/B05-ci-sentry-isolation` (`f16466e`), not the author of the fix.
**Fix PR:** https://github.com/aupchurch167/proof/pull/19
**Batch source:** `audit/batches/B05.md` on `cursor/audit-pre-launch-30f7` (PR #9)
**App code changed by this verification:** none.

## Overall recommendation: **merge**

CI exists and is green on this PR. A broken expect exits 1. The named Friday isolation cases (WH-1, V-01, C-02, O-03, L-02) assert tenant boundaries, not smoke. Invalid / expired portal tokens are **401**, not 404. `cleanupTestData` refuses a production-shaped URL without `PROOF_TEST_DB=1`; `keep-me` survives that refuse; cleanup still wipes when the guard is satisfied. Sentry is optional: no DSN → app boots; fake DSN init does not throw at require; no DSN or auth token is committed; PII scrub is present; the client Vite build succeeds with `VITE_SENTRY_DSN` unset.

Do **not** send back. Residuals below are the ones the batch / STATUS already scoped (required GitHub check is owner-owned; full isolation matrix still open; cleanup still truncates once the guard passes; Sentry projects/DSNs are created after merge) plus a few incomplete-cover follow-ups (`_test` substring, whitespace DSN, client scrub weaker than Node). None restore “no CI”, a hollow isolation suite, portal 404-on-garbage-token, an unguarded wipe, or a boot crash without Sentry.

---

## Method

1. Read original findings on `origin/cursor/audit-pre-launch-30f7` (`audit/findings/A6.md`, `A9.md`, `audit/batches/B05.md`, `audit/tenant-isolation-tests.md`).
2. Reviewed PR #19 diff vs `main` (18 files, +801/−4). Scope matches the batch. Existing `portal.test.js` / `b02-auth-portal.test.js` / `webhooks-inbound.test.js` were **not** edited. `cois.test.js` only **adds** C-02. `setup.js` adds the wipe guard; `deleteMany({})` is unchanged once the guard passes. No weakened assertions. No secrets in the tree.
3. Re-ran official suites on this branch (local Postgres 16 + `prisma migrate deploy` of **existing** migrations only — 18 files, none added by B05):

```
cd server && npm test
cd server && npm run test:friday
cd client && npm run build   # VITE_SENTRY_DSN unset
```

**Result:** 26 suites, **291 passed**. Friday: 11 suites, **144 passed**. Client production build succeeded (418 modules, no DSN in `dist/`).

4. Adjacent probes (not committed): broken `expect(1).toBe(2)` → Jest exit 1; `PROOF_TEST_DB=true` / `yes` / hyphen `proof-test` / db name `test`; `_test` in username / hostname / password; keep-me then successful wipe; V-02/V-03/C-01/C-03/C-05; O-03 body leak of B’s token; L-02 GET+POST UUID body; L-01 slug; portal PUT/POST expired + wrong secret + `alg=none`; `require('./src/app')` with no DSN and with a fake DSN; scrub of nested email / auth headers / query string; `git grep` for ingest DSNs / `sntrys_` / `ghp_` / `sk_live_`.

---

## Per-finding verdicts

### A9-01 — No CI workflow — **Verified**

**Original:** no `.github/`, no workflow. A PR could merge a broken tenant filter. Test: open a PR that fails an expect → CI red.

**This branch:** `.github/workflows/test.yml` runs on every `pull_request` and on `push` to `main`. Node 22, root `npm ci`, Postgres 16 service (`proof_test`), `prisma migrate deploy`, `cd server && npm test`, then `cd client && npm run build` with `VITE_SENTRY_DSN: ""`. No `continue-on-error`. No path filters. YAML parses (`python3` PyYAML).

```1:65:.github/workflows/test.yml
name: Test

on:
  pull_request:
  push:
    branches: [main]
# ...
      - name: Server tests
        working-directory: server
        run: npm test
      - name: Client production build
        working-directory: client
        run: npm run build
        env:
          VITE_SENTRY_DSN: ""
```

**GitHub Actions on PR #19 (this SHA `f16466e`):** **green** — [Test / Server suite + client build](https://github.com/aupchurch167/proof/actions/runs/35548283642/job/106178130825). Log: Postgres 16 service, `npm ci`, 18 migrations applied, `jest --runInBand --forceExit --detectOpenHandles`, **26 passed / 291 passed**, then Vite production build with `VITE_SENTRY_DSN` empty (`✓ built in 3.07s`). Prior runs on `569d5e9` and `326963b` also succeeded.

**Repro (how a broken expect fails CI):** throwaway `tests/_broken.expect.test.js` with `expect(1).toBe(2)` → Jest **exit 1** (`Expected: 2 / Received: 1`). Deleted after. The Actions step uses default `set -e`, so that exit fails the job.

**Tried to break it:**

| Case | Result |
|---|---|
| Workflow `on.pull_request` unfiltered | yes — this PR triggered three successful runs |
| Workflow `on.push` includes `main` | yes |
| Job points `DATABASE_URL` at `…/proof_test` + `PROOF_TEST_DB=1` | yes (A9-05) |
| `continue-on-error` / path ignore that would skip tests | **absent** |
| Branch protection requires this check | **could not read** (`gh` 403). Owner residual, already in STATUS |

**Residual:** making the check required on `main` is owner-owned. The workflow file is in-repo; a merge without the required-status toggle can still land if GitHub is not told to block. Not send-back for “no CI workflow.”

---

### A9-02 — Session API has almost no two-tenant tests — **Verified**

**Original:** session routes were single-org happy paths. Named launch cases: WH-1, V-01, C-02, O-03, L-02 plus the Friday file list. Full `/audit/tenant-isolation-tests.md` matrix was out of scope.

**This branch:** Friday files are listed in `server/package.json` `test:friday` and are inside CI via full `npm test`. Named cases:

| ID | File | Assertion (not smoke) |
|---|---|---|
| **WH-1** | `webhooks-inbound.test.js` | Shared vendor email; last `UPLOAD_REQUEST` on A → COI + `vendor_reply` audit **only on A**; B stays 0 |
| **V-01** | `b02-auth-portal.test.js` | `GET /api/vendors` as A: every row `orgId === orgA`, **no** `vendorB`, **no** `uploadToken` |
| **C-02** | `cois.test.js` (added here) | `GET /api/cois/:id` as A against B’s COI → **404**; body has no `id` / `pdfPath` / `orgB.id` |
| **O-03** | `b02-auth-portal.test.js` | MEMBER usage has **no** `portalPreviewToken`; ADMIN token is minted, **not** `vendorB.uploadToken` |
| **L-02** | `b02-auth-portal.test.js` | `GET /api/apply/:orgB.id` → **404** |

**Repro (official):** Friday suite **11 / 144 passed**, including the five named cases. Full suite **26 / 291**.

**Tried to break it:**

| Case | Result |
|---|---|
| V-01 list includes vendorA, excludes B token / orgB id | **yes** |
| V-02 `GET /vendors/:vendorB` as A | **404**, no vendor name |
| V-03 `PUT /vendors/:vendorB` `{name:'Hacked'}` | **404**; B name unchanged |
| C-01 `GET /api/cois` as A | **no** `coiB.id` |
| C-02 body leak of `secret-b.pdf` / orgB / vendor B name | **absent** |
| C-03 `GET /cois/:id/pdf` as A | **404**, no URL |
| C-05 approve B’s COI as A | **404**; status still `APPROVED` |
| O-03 MEMBER / ADMIN body contains `vendorB.uploadToken` | **no** |
| O-03 org B usage vendor count | **1** (not A’s) |
| L-02 GET + POST UUID | **404**; GET body has no org name / slug |
| L-01 `GET /api/apply/:slugB` | **200** B name only |

**Test quality:** none of the five is `expect(res.status).toBe(200)` alone. WH-1 checks per-org row counts. C-02 checks body leak. V-01 / O-03 check token absence. L-02 is the A2-05 regression.

**Residual (already in STATUS):** the rest of `/audit/tenant-isolation-tests.md` (V-04…, C-04…, reports, users, settings, cron, v1 extras) is still open. Not send-back.

---

### A9-03 — portal.test.js expects 401; portal.js returns 404 — **Verified**

**Original:** tests expected 401 for `invalid-token-string`; `portal.js` returned 404 without `jwt.verify`. Suite was red or skipped.

**This branch (aligned on main after B02, unchanged here):** `loadPortalVendor` catches `verifyUploadToken` failure and missing `vendorId` → **401**. Valid JWT for a missing vendor → **404** (resource, not token). Tests still expect 401 for invalid / UUID / expired / wrong purpose. **No test file was weakened.**

```18:37:server/src/routes/portal.js
async function loadPortalVendor(req, res, query = {}) {
  let payload;
  try {
    payload = verifyUploadToken(req.params.uploadToken);
  } catch (err) {
    res.status(401).json({ error: 'Upload link has expired or is invalid', orgName: null });
    return null;
  }
  // ...
  if (!vendor) {
    res.status(404).json({ error: 'Upload link has expired or is invalid', orgName: null });
    return null;
  }
```

**Repro (official):** `portal.test.js` green inside Friday + full suite. Logs: `GET /api/portal/invalid-token-string` **401**; expired JWT **401**; UUID **401**; missing vendor **404**.

**Tried to break it:**

| Case | Result |
|---|---|
| GET invalid string | **401** `/expired\|invalid/i` |
| GET expired JWT | **401** |
| PUT `/info` expired / invalid | **401**; vendor name **unchanged** |
| POST `/upload` expired / invalid | **401** |
| Wrong `JWT_SECRET` | **401** |
| `alg=none` | **401** |
| Valid JWT, vendor 0000… | **404** (oracle residual; token verified) |

**Residual:** 404 on a *valid* upload JWT for a deleted / unknown vendor can confirm “this id was a vendor once” if an attacker can mint JWTs (they cannot without `JWT_SECRET`). Same as B02. Not send-back.

---

### A9-05 — Jest setup deletes every row in every table — **Verified**

**Original:** `cleanupTestData` `deleteMany({})` with no org filter and no test-DB gate. `npm test` against a copied prod URL would wipe customers. Test: `keep-me` survives, or suite refuses without `PROOF_TEST_DB=1`.

**This branch:** wipe runs only when `PROOF_TEST_DB === '1'` **or** `DATABASE_URL` contains `_test`. Official `cleanup-guard.test.js` plants `keep-me`, points env at a production-shaped URL, expects the refuse message, then asserts the row is still there.

```112:121:server/tests/setup.js
function isSafeTestDatabase(env = process.env) {
  if (env.PROOF_TEST_DB === '1') return true;
  const url = env.DATABASE_URL || '';
  return url.includes('_test');
}

async function cleanupTestData() {
  if (!isSafeTestDatabase()) {
    throw new Error(UNSAFE_CLEANUP_MESSAGE);
  }
```

**Repro (official):** 4 cleanup-guard tests passed in the 291. Probe: refuse without flag/`_test`; `keep-me` still present; restore flag + `_test` URL → `cleanupTestData` deletes `keep-me`.

**Tried to break it:**

| Case | Guard |
|---|---|
| `PROOF_TEST_DB=1` + `…/proof` | **allows** (explicit override) |
| `…/proof_test` no flag | **allows** |
| `…/proof` no flag | **refuses**; keep-me lives |
| `PROOF_TEST_DB=true` / `yes` | **refuses** (strict `'1'`) |
| `…/proof-test` (hyphen) | **refuses** |
| `…/test` (no `_test`) | **refuses** |
| empty URL | **refuses** |
| `_test` in **username** / **hostname** / **password** | **allows** — substring residual |
| After restore, cleanup | **wipes** keep-me (tests still work) |

**Residual (already in STATUS):** once the guard passes, every table is still truncated. `PROOF_TEST_DB=1` against a shared/prod URL is an operator foot-gun. `_test` anywhere in the URL string (user, host, password, query) satisfies the check. Finding sketch was substring-based (`contain test`); they used `_test`, which is tighter than `test` but still not “parse the database name.” Not send-back.

---

### A6-01 — No error tracking product — **Verified**

**Original:** grep `Sentry|sentry|datadog|…` → no matches. Production 500s were `console.error` only. Fix: `@sentry/node` + `@sentry/react`; capture Express errors.

**This branch:** Node init only when `SENTRY_DSN` is set; React init only when `VITE_SENTRY_DSN` is set. `sendDefaultPii: false`, `tracesSampleRate: 0`, `beforeSend` scrubs emails and auth/cookie/`x-api-key`/`x-proof-signature` headers. Release from `SENTRY_RELEASE` else Railway SHA else `GITHUB_SHA`. Helmet `connect-src` allows Sentry ingest. Both `.env.example` files document empty optional vars. `index.js` loads `instrument.js` before `validateEnv` (no-op without DSN). `attachSentry` is after routes and before the JSON error handler.

```58:75:server/src/lib/sentry.js
function initSentry(env = process.env) {
  if (!isSentryEnabled(env)) return false;
  if (global.__PROOF_SENTRY_INIT) return true;

  const Sentry = require('@sentry/node');
  Sentry.init({
    dsn: env.SENTRY_DSN,
    sendDefaultPii: false,
    tracesSampleRate: 0,
    beforeSend(event) {
      return scrubSentryEvent(event);
    },
  });
```

**Repro (official):** `sentry.test.js` — no DSN → `initSentry` false + `require('../src/app')` does not throw; release tagging; scrub of email / auth headers / token keys. Client `npm run build` with `VITE_SENTRY_DSN` unset: **ok**. CI same.

**Tried to break it:**

| Case | Result |
|---|---|
| `require('./src/app')` no `SENTRY_DSN` | boots; `isSentryEnabled` false |
| `require('./instrument')` no DSN | no-op; `validateEnv` still ok |
| `SENTRY_DSN=https://public@o0.ingest.sentry.io/0` then `initSentry` / `require('./src/app')` | **did not throw** |
| `SENTRY_DSN=''` | disabled |
| `SENTRY_DSN='   '` (whitespace) | **truthy** — would call `Sentry.init` (residual) |
| Scrub: user email/ip/username, `Authorization`/`Cookie`/`X-Api-Key`/`X-Proof-Signature`, nested `email`, query `email=`, `phone`/`ssn` keys | redacted; `user.id` and `content-type` kept |
| `git grep` ingest DSN / `sntrys_` / `ghp_` / `sk_live_` / `SENTRY_DSN=https` | **no matches** (lockfile is registry tarballs only) |
| Tracked env files | `server/.env.example`, `client/.env.example` only; DSN lines empty |
| `client/dist` after build | **no** ingest host / DSN |
| Workflow JWT values | test strings (`test-jwt-secret-which-is-32-chars!!`), not production |

**Residual (already in STATUS):** live “throw in a test route; event in Sentry” needs owner-created projects after merge. Until DSNs are set, 500s still go to `console.error`. Client `beforeSend` redacts emails on `user`/`message` only — not request headers/cookies. Request bodies that are not email/token-shaped keys are not fully stripped. Helmet `connect-src` allows `https://*.sentry.io` even when no DSN is configured (CSP widening so a future XSS could POST there). Whitespace-only `SENTRY_DSN` is truthy. Not send-back.

---

## Also-check answers

| Question | Answer |
|---|---|
| A9-01: workflow on PR + main; Postgres service; migrate deploy; server tests; client build; job fails on red? CI green on this PR? | **Yes / yes.** `on.pull_request` + `push: main`. Postgres 16 `proof_test`. `prisma migrate deploy` then `npm test` then client build. Broken expect → exit 1. PR #19 check **success** on `f16466e` (291/291 + Vite). |
| A9-02: WH-1, V-01, C-02, O-03, L-02 actually assert isolation? Friday suite listed and runnable? Hollow tests? | **Yes / yes / no.** See table. `npm run test:friday` → 11/144. Adjacent V-02/V-03/C-01/C-03/C-05/L-01 also fail closed. |
| A9-03: invalid/expired portal token → 401 (not 404)? | **Yes.** GET/PUT/POST. Wrong secret and `alg=none` also 401. Valid JWT + missing vendor is 404 (unchanged B02 residual). |
| A9-05: cleanup throws without `PROOF_TEST_DB=1` and without `_test` in URL? `keep-me` survives when guard refuses? With guard satisfied, cleanup still works? | **Yes / yes / yes.** Official + probe. `true`/`yes`/hyphen/`test` refuse. `_test` in user/host/password is the substring residual. |
| A6-01: no DSN → app boots, no crash? Fake DSN init path doesn’t throw at require? No secrets in repo? PII scrub present? Client Vite build without `VITE_SENTRY_DSN`? | **Yes / yes / yes / yes / yes.** |
| Sentry DSN or auth tokens committed? | **No.** Examples are empty. Workflow JWTs are test fixtures. Bundle and lockfile have no ingest DSN. |

---

## PR diff review

- **Scope:** on-batch. New workflow, wipe guard + test, C-02, `test:friday` script, `@sentry/node` + `@sentry/react`, `instrument.js` / `lib/sentry.js` / `client/src/sentry.js`, helmet `connect-src`, `.env.example` docs, STATUS notes. No migrations.
- **Regressions:** none observed. Official 291 includes B01–B04 suites. Portal 401/404 split unchanged. Cleanup still truncates when allowed (intentional).
- **Weakened tests:** none. No existing assertion was relaxed. `cois.test.js` only added C-02.
- **Secrets:** none committed. No live DSN, no `ghp_`, no production JWT.
- **Scope creep:** helmet allows `*.sentry.io` even without a DSN (CSP widening, not product behavior). Sentry packages in the lockfile. Fine.
- **New issues (non-blocking):** `_test` substring; whitespace DSN; client scrub thinner than Node; required GitHub check not confirmable from here.

---

## Backlog

Full triage backlog lives on `cursor/audit-pre-launch-30f7` (PR #9) as `audit/backlog.md` and is **not** on this branch.

Suggested backlog rows if PR #9 lands:

| ID | After V |
|---|---|
| A9-01 | **Verified** (residual: require the check on `main` — owner) |
| A9-02 | **Verified** (residual: rest of tenant-isolation matrix) |
| A9-03 | **Verified** (residual: 404 on valid JWT + missing vendor) |
| A9-05 | **Verified** (residual: full truncate once guard passes; `_test` substring; `PROOF_TEST_DB=1` override) |
| A6-01 | **Verified** (residual: no live Sentry project until owner sets DSNs; client scrub; whitespace DSN; CSP connect-src) |

---

## Merge vs send-back

**Merge PR #19.**

Reasons:

1. GitHub Actions is green on this SHA: Postgres, migrate, 291 server tests, client build with empty `VITE_SENTRY_DSN`. A broken expect exits 1.
2. Named isolation cases assert the isolation property. Adjacent IDOR (vendor GET/PUT, COI list/pdf/approve, apply UUID POST) fail closed.
3. Invalid / expired portal tokens are 401 on GET, PUT, and upload.
4. Cleanup refuses a prod-shaped URL; `keep-me` survives; tests still wipe when the guard is on.
5. App boots with no DSN; fake DSN does not throw at require; no DSN/token in the repo; PII scrub works.
6. Residuals are the batch’s own scope or follow-ups that do **not** restore the original findings.

**Do not silently patch** required-check enablement, the full isolation matrix, `_test` URL parsing, client header scrub, or a live Sentry test project in this verify PR.
