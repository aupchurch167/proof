# B06 verification (Prompt V)

**Verifier:** skeptical re-run against `fix/B06-legal-product` (`151b063`), not the author of the fix.
**Fix PR:** https://github.com/aupchurch167/proof/pull/21
**Batch source:** `audit/batches/B06.md` on `cursor/audit-pre-launch-30f7` (PR #9)
**App code changed by this verification:** none.

## Overall recommendation: **merge**

`/terms` and `/privacy` exist (server HTML + SPA routes) and are marked **DRAFT / not counsel-approved**. Password signup and first-time Google signup require `acceptTerms`; missing / false / `1` / `yes` / `TRUE` / `on` → **400** and no user row. Acceptance columns are written with version `draft-2026-09-21`. Existing login / existing Google / accept-invite are **not** forced (residual, already in STATUS). Public apply with a `w9` file or field → **400** `W9_NOT_ACCEPTED` and no vendor; the apply UI has no upload; existing `w9Path` is kept; GET vendor with a signed W-9 writes `w9:download`; authenticated `POST /vendors/:id/documents` still accepts W-9s. Settings billing is invoiced / not self-serve (dead Upgrade CTA gone). Requirements “New template” is Coming soon; period toggles say stored-not-enforced. Verify banner no longer claims “unlock all features.” `LICENSE` + `NOTICE` are proprietary. Privacy draft names GIS, localStorage, Resend, Anthropic, Spaces, and the other system-map processors.

Do **not** send back. Residuals below are the batch’s own scope (counsel final drafts, W-9 DPA/KMS, invitees not asked to accept, `npm run licenses` does not walk the workspace hoist) plus a few incomplete-cover follow-ups (alternate multipart field names 500 instead of `W9_NOT_ACCEPTED`; Sentry not named in the draft). None restore “no ToS URL,” public apply storing a W-9, a self-serve Upgrade button, or a fake-final legal page.

---

## Method

1. Read original findings on `origin/cursor/audit-pre-launch-30f7` (`audit/batches/B06.md`, `audit/findings/A10.md`, `audit/findings/A8.md`, `audit/system-map.md`).
2. Reviewed PR #21 diff vs `main` (30 files, +749/−37). Scope matches the batch. Existing `auth.test.js` / `b02-auth-portal.test.js` only **add** `acceptTerms: true` so signup still exercises the original assertions. Existing Google link / unverified-email cases still omit `acceptTerms`. No secrets. Legal bodies are labeled DRAFT; section 5 refuses to invent a warranty disclaimer.
3. Re-ran official suites on this branch (local Postgres 16 + `prisma migrate deploy` of **19** migrations, including additive `20260921000000_add_legal_acceptance`):

```
cd server && npm test
cd client && npm run build   # VITE_SENTRY_DSN unset
```

**Result:** 27 suites, **301 passed** (B05 baseline 26 / 291 + B06). Client production build succeeded (421 modules).

4. Adjacent probes (not committed): signup `acceptTerms` `false` / `1` / `yes` / `TRUE` / `on` / `""`; string `"true"` stores versions; login + existing Google + accept-invite leave null columns; first-time Google `1`/`yes`/`TRUE`/`on`/`false` → 400; apply `w9` file / empty field / JSON `w9`; `w9Path` body injection; alternate file names `W9` / `W-9` / `w_9` / `w9File` / `taxForm`; GET vendor without `w9Path` (no audit); authenticated documents upload; `npm run licenses`; client vs server legal lockstep; bundle grep for leftover CTAs; browser of `/signup` `/terms` `/privacy` `/login`.

---

## Per-finding verdicts

### A10-01 — No Terms of Service or Privacy Policy — **Verified**

**Original:** grep found no policy pages and no signup checkbox. Test: signup without checkbox → 400; pages return 200.

**This branch:** public `/terms` and `/privacy` as Express HTML (crawlers / tests / production) and as React routes. Signup checkbox (HTML `required`) plus server `validate('signup')` refine `true` / `'true'`. First-time Google is gated in the handler (`acceptedLegal`); schema leaves `acceptTerms` optional so **existing** Google users can still sign in. New password / first-time Google users get `legalAcceptanceFields()` (`termsAcceptedAt`, `termsVersion`, `privacyAcceptedAt`, `privacyVersion` = `draft-2026-09-21`). Copy is a placeholder: banner “DRAFT — not counsel-approved… not a final legal document.”

```20:29:server/src/utils/validation.js
  signup: z.object({
    orgName: z.string().min(1, 'Organization name is required'),
    email: z.string().email('Invalid email format'),
    password: passwordSchema,
    firstName: z.string().min(1, 'First name is required'),
    lastName: z.string().min(1, 'Last name is required'),
    acceptTerms: z.any().refine(
      (v) => v === true || v === 'true',
      'You must accept the Terms of Service and Privacy Policy'
    ),
```

```228:232:server/src/routes/auth.js
    if (!user) {
      if (!acceptedLegal(req.body.acceptTerms)) {
        return res.status(400).json({
          error: 'You must accept the Terms of Service and Privacy Policy to create an account.',
        });
```

**Repro (official):** signup missing / `false` → 400 `/terms|privacy/i`; `true` → 201 and versions stored; `GET /terms` and `GET /privacy` 200 + DRAFT; Google without `acceptTerms` → 400 and no user. Inside the 301.

**Tried to break it:**

| Case | Result |
|---|---|
| Signup omit / `false` / `0` / `1` / `yes` / `TRUE` / `on` / `""` | **400**, no user row |
| Signup `acceptTerms: "true"` | **201**, versions stored |
| First-time Google `1` / `yes` / `TRUE` / `on` / `false` / omitted | **400**, no user |
| Login of user with null acceptance | **200**; columns stay **null** |
| Existing Google user, no `acceptTerms` | **200**; columns stay **null** (not backfilled) |
| `POST /auth/accept-invite` | **200**; all four columns **null** |
| `GET /terms` / `/privacy` | 200 HTML, `DRAFT_BANNER`, version `draft-2026-09-21` |
| Fake-final legalese (`hereby agrees`, invented liability cap) | **absent**; §5 says counsel must rewrite |
| Client vs server `TERMS_*` / `PRIVACY_*` / `DRAFT_BANNER` | **lockstep** |
| Browser `/signup` | checkbox unchecked; submit without it → native “Please check this box…” |
| Browser `/terms` `/privacy` | DRAFT banner **once**; version shown |
| Browser `/login` | Terms/Privacy links; **no** checkbox |

**Residual (already in STATUS):** existing users and **invitees** are not forced to accept. First-time Google from `/login` has no checkbox and fails closed (400). Counsel still replaces the bodies. Not send-back.

---

### A10-02 — W-9 on public apply (eng half) — **Verified**

**Original:** public `POST /api/apply/:slug` stored `w9Path` on the same Spaces prefix; GET vendor signed the URL with no audit. Test: apply without auth cannot upload `w9`; signed W-9 URL writes an audit row.

**This branch:** apply still **declares** the multer `w9` field so a file or body key can be seen, then returns **400** `W9_NOT_ACCEPTED` before `vendor.create`. `uploadFile` is not called. UI removed the file input and IRS blank-form link; note says not collected. Existing `w9Path` is not deleted. `GET /api/vendors/:id` `logAudit`s `entity=w9` `action=download` only when a signed URL was created. Authenticated `POST /vendors/:id/documents` still writes `w9s/`.

```75:80:server/src/routes/apply.js
      if (req.files?.w9?.length || Object.prototype.hasOwnProperty.call(req.body || {}, 'w9')) {
        return res.status(400).json({
          error: 'W-9 uploads are not accepted on the public apply form until a DPA is in place.',
          code: 'W9_NOT_ACCEPTED',
        });
      }
```

**Repro (official):** `w9` file → 400 `W9_NOT_ACCEPTED`, vendor null; `w9` field → same; apply without w9 → 201 and existing `w9Path` kept; GET vendor with path → `w9Url` + audit row. Passed.

**Tried to break it:**

| Case | Result |
|---|---|
| Multipart `w9` PDF | **400** `W9_NOT_ACCEPTED`; `uploadFile` **not** called; no vendor |
| Empty field `w9=` | **400** `W9_NOT_ACCEPTED` |
| JSON `{ w9: "sneaky" }` | **400** `W9_NOT_ACCEPTED` |
| Body `w9Path=w9s/injected.pdf` | **201**; stored `w9Path` **null** (zod strip + handler allow-list) |
| File fields `W9` / `W-9` / `w_9` / `w9File` / `taxForm` | multer **Unexpected field** → **500**; **no** vendor; **no** `w9Path` (fail-closed for storage) |
| Apply UI `type="file"` for W-9 / `irs.gov/.../fw9` | **absent** (source + `client/dist` grep) |
| GET vendor **without** `w9Path` | 200; `w9Url` falsy; **no** `w9:download` row |
| `POST /api/vendors/:id/documents` + `w9` (authenticated) | **200**; `w9Path` set |
| Vendors “Add vendor” drawer | still has W-9 file input (authenticated path; expected) |

**Residual (already in STATUS):** authenticated W-9 still same Spaces prefix / no KMS; product decision on when public apply may collect again; alternate field names are 500 not a typed 400 (not stored). Not send-back.

---

### A8-02 — Advertised settings incomplete / banner honesty — **Verified**

**Original:** Requirements “New template” disabled with an engineering tooltip; Plan & billing was a paragraph plus a dead “Upgrade to Unlimited” button; email banner “unlock all features” was false; `warnInPeriod` / `retainForPeriod` stored and unused.

**This branch:**

- Settings Plan & billing chip: **Invoiced · not self-serve**. Copy: no checkout; plan is set in the database; contact us to invoice. Sidebar meter “Upgrade to Unlimited” **removed** → “No self-serve upgrade… we'll invoice you.”
- Requirements button: **New template · Coming soon** (`disabled` while `multiTemplateSupported` is false). Helper text: server does not store additional templates. Period toggles have explicit “not enforced” hints.
- Banner: “Verify your email to use Proof… This does not unlock billing or per-trade templates — those are not self-serve yet.”

**Repro:** `git grep` / production bundle:

| Needle | `client/src` + `client/dist` |
|---|---|
| `unlock all features` | **absent** |
| `Upgrade to Unlimited` | **absent** |
| `Invoiced · not self-serve` | present |
| `Coming soon` (templates) | present |
| `not enforced` / “Saved on the coverage period only” | present |

Did **not** browser Settings / Requirements (those routes are behind `PrivateRoute`). Source + built JS are the evidence.

**Residual (already in STATUS):** self-serve billing and per-trade templates remain unbuilt. Toggles still persist the unused flags. Not send-back.

---

### A10-04 — No LICENSE / dependency inventory — **Verified**

**Original:** no root LICENSE / NOTICE. Fix sketch: add LICENSE; `license-checker --production` output in `audit/` or `docs/licenses.md`.

**This branch:** root `LICENSE` + `NOTICE` are **proprietary / all rights reserved** (the repo never implied OSS). `docs/licenses.md` explains how to generate a production list and names the known runtimes. Root script `npm run licenses` is `npx license-checker --production --excludePrivatePackages --summary`.

**Repro:** files present. LICENSE does not grant MIT/Apache rights.

**Tried to break it:**

| Command | Result |
|---|---|
| `test -f LICENSE && test -f NOTICE && test -f docs/licenses.md` | **yes** |
| `npm run licenses` (repo root) | **errors** `No packages found in this path` (root is private, no production deps; workspaces hoisted) |
| `cd server && npx license-checker --production --excludePrivatePackages --summary` | `MIT: 1` — does **not** walk hoisted `node_modules` |
| Also-check “docs/licenses.md **or** npm run licenses works” | **docs/licenses.md exists** |

**Residual:** the convenience script does not produce a questionnaire CSV on this workspace layout. STATUS already said “document `license-checker`,” not “commit a dump.” Counsel still approves the app LICENSE. Not send-back — the finding was missing LICENSE/NOTICE plus an inventory path.

---

### A10-05 — Privacy must name GIS / localStorage / processors — **Verified**

**Original:** no cookie banner; tokens in localStorage; GIS may set Google cookies. Test: privacy lists GIS + localStorage + Resend + Anthropic + Spaces. Counsel call on a banner.

**This branch:** privacy §2–4 name those five plus hosted PostgreSQL, optional Helm Core, Railway, Nginx / Let’s Encrypt, Airtable-as-import-only, and “not a cookie banner / not ‘essential cookies only’.” HIPAA: “not intended for PHI.” Official `GET /privacy` asserts the five named processors. Browser `/privacy` showed the same list. Server HTML escapes `Let’s` as a unicode apostrophe (still present).

**Residual (already in STATUS):** cookie-banner decision is counsel’s. Live host (Neon vs other Postgres, Railway vs droplet, Helm on/off) is not pinned. **Sentry** (B05, optional DSN) is not in the draft — it was not on the system map this batch cited. Not send-back.

---

## Also-check answers

| Question | Answer |
|---|---|
| A10-01: `/terms` `/privacy` render DRAFT markers? | **Yes.** Server HTML + SPA. Banner once. Version `draft-2026-09-21`. |
| Password + first Google signup require `acceptTerms`? | **Yes.** 400 without it; versions stored when true. |
| Acceptance cols set with version? | **Yes.** All four columns. |
| No acceptance → 400? | **Yes** on password signup and first-time Google. |
| Existing users not forced (document residual)? | **Yes.** Login, existing Google, accept-invite leave nulls. Documented. |
| A10-02: apply w9 file or field → 400 `W9_NOT_ACCEPTED`? | **Yes.** Also empty field and JSON `w9`. |
| UI has no upload? | **Yes.** Apply only has a COI file input. |
| Existing `w9Path` not deleted? | **Yes.** Official + probe. |
| GET vendor with signed W-9 URL writes audit? | **Yes.** `entity=w9` `action=download`. Without path: no row. |
| Authenticated vendor doc upload still OK? | **Yes.** Probe 200; Add-vendor drawer still has the input. |
| A8-02: Settings billing not self-serve upgrade CTA? | **Yes.** Chip + copy; Upgrade button gone (bundle grep). |
| Requirements templates coming soon? | **Yes.** Button + helper. |
| Verify banner doesn’t claim unlock-all-features? | **Yes.** Phrase gone from src and `dist`. |
| A10-04: LICENSE/NOTICE present; docs/licenses.md or npm run licenses works? | **Yes / docs exist.** Script itself errors on this hoist. Residual. |
| A10-05: Privacy mentions GIS, localStorage, Resend, Anthropic, Spaces (and other map processors)? | **Yes.** Plus Postgres, Helm Core, Railway, Nginx/LE, Airtable. |
| CI green on PR if available? | **Yes.** Latest SHA `151b063` — [Test / Server suite + client build](https://github.com/aupchurch167/proof/actions/runs/35551885805) **success**. Also green on `6ec4221` and `220a9c5`. |

---

## PR diff review

- **Scope:** on-batch. Legal pages + acceptance columns, apply W-9 gate + download audit, product-honesty copy, LICENSE/NOTICE/docs, one test file, STATUS. Login footer links are in-scope for A10-01.
- **Regressions:** existing signup tests now send `acceptTerms: true` so they still hit the original password-policy cases — not a weakened assertion. Official 301 includes B01–B05 suites.
- **Weakened tests:** none. Google existing-user / unverified-email cases still omit `acceptTerms` (they must).
- **Secrets:** none. No DSN, no `ghp_`, no `sk_live_`.
- **Fake-final legal:** no. Banner, “(placeholder)”, §5 refuses a liability clause, checkbox says “draft — not counsel-approved.”
- **Migration:** `20260921000000_add_legal_acceptance` — four **nullable** `User` columns, no `NOT NULL`, no backfill, no index rewrite. `\d "User"` after `migrate deploy`: `termsAcceptedAt` / `privacyAcceptedAt` `timestamp(3)` nullable; version columns `text` nullable. Additive-safe.
- **Scope creep:** duplicate client/server legal modules (lockstep today; drift risk). `w9:download` audit phrase. Fine.
- **New issues (non-blocking):** invite path skips acceptance; `npm run licenses` broken on workspace hoist; alternate apply file names 500; Sentry unnamed in the draft.

---

## Backlog

Full triage backlog lives on `cursor/audit-pre-launch-30f7` (PR #9) as `audit/backlog.md` and is **not** on this branch.

Suggested backlog rows if PR #9 lands:

| ID | After V |
|---|---|
| A10-01 | **Verified** (residual: invitees / existing users not forced; counsel replaces DRAFT; `/login` Google has no checkbox and fails closed) |
| A10-02 | **Verified** (eng half; residual: authenticated W-9 + same prefix / no KMS; DPA product decision; alternate field names 500) |
| A8-02 | **Verified** (residual: billing and per-trade templates still unbuilt) |
| A10-04 | **Verified** (residual: `npm run licenses` does not inventory hoisted workspace deps) |
| A10-05 | **Verified** (residual: cookie-banner counsel call; confirm live subprocessors; Sentry optional) |
| A10-03 | unchanged — B07+ DSAR / erase |

---

## Merge vs send-back

**Merge PR #21.**

Reasons:

1. Official B06 cases pass on this SHA: signup 400/201 + versions, `/terms` `/privacy` DRAFT, apply `w9` 400, existing `w9Path` kept, W-9 download audit. Full suite **27 / 301**. Client build ok.
2. Break attempts fail **closed** (or match a documented residual). Existing users are not forced.
3. Product copy no longer advertises self-serve upgrade, working per-trade templates, or “unlock all features.”
4. LICENSE/NOTICE are proprietary; privacy names the required processors; legal text is marked DRAFT.
5. Migration is additive nullable columns. CI green on the PR. Verifier did not change app code.

**Do not silently patch** invite acceptance, the licenses script, alternate-field 400 mapping, or Sentry-in-privacy in this verify PR.
